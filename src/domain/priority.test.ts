import { describe,expect,it } from "vitest";import { PRIORITY_TEST_VECTOR,PRIORITY_TEST_VECTOR_EXPECTED,calculatePriority } from "./priority";
describe("priority-v1",()=>{it("matches the golden vector and explains all ten factors",()=>{const result=calculatePriority(PRIORITY_TEST_VECTOR);expect(result.audit.modelScore).toBe(PRIORITY_TEST_VECTOR_EXPECTED.modelScore);expect(result.audit.effectiveScore).toBe(56.98);expect(result.audit.effectiveBand).toBe("high");expect(result.audit.factors).toHaveLength(10);expect(result.audit.factors.reduce((s,f)=>s+f.contribution,0)).toBeCloseTo(result.audit.modelScore,2)});it("keeps missing values explicit",()=>{const result=calculatePriority({reportId:"missing",observedAt:"2026-01-01T00:00:00.000Z",calculatedAt:"2026-01-01T00:00:00.000Z",verifiedSpecialWaste:"none",trafficLaneBlocked:false});expect(result.audit.missingFactors.length).toBeGreaterThan(0);expect(result.audit.requiresManualReview).toBe(true);expect(result.audit.modelScore).toBe(0)});it("applies the documented traffic safety floor",()=>{const result=calculatePriority({...PRIORITY_TEST_VECTOR,reportId:"lane",obstruction:"traffic_lane",trafficLaneBlocked:true});expect(result.audit.effectiveScore).toBeGreaterThanOrEqual(90);expect(result.audit.safetyEscalation.kind).toBe("blocked_traffic_lane")})});
